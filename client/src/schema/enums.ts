export enum DEVICESTATUS {
  Online = "online",
  Offline = "offline",
}

export enum DOWNLOADSTATUS {
  FAIL = "failed",
  CANC = "cancelled",
  DOWN = "downloading",
  DOWND = "downloaded",
  SCHE = "scheduled",
}

export enum BILLINGCYCLE {
  MONTHLY = "monthly",
  QUATERLY = "quarterly",
  ANNUALY = "annual",
}
